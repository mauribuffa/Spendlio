Shimmer loading placeholder — Spendlio prefers skeletons over spinners. `SkeletonRow` matches a `TransactionRow`.

```jsx
<Skeleton width={120} height={20} />
<Skeleton circle width={40} />
<SkeletonRow />
```
