On/off toggle — settings rows, "Split evenly", "Recurring", notification preferences. Green when on.

```jsx
<Switch checked={recurring} onChange={e=>setRecurring(e.target.checked)} label="Repeat monthly" />
```
