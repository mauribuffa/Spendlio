Styled native dropdown — category, account, currency, or group pickers.

```jsx
<Select label="Category" value={cat} onChange={e=>setCat(e.target.value)}
  options={['Groceries','Dining','Transport','Housing']} />
```
