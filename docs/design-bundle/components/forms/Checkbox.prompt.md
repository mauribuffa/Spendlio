Checkbox — multi-select list rows, "who's in this split", agreeing to terms.

```jsx
<Checkbox checked={inSplit} onChange={...} label="Maya Okafor" round />
```

`round` renders a circular check for people-selection lists.
