{
  "name": "@spendlio/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=../../.env --import tsx src/main.ts",
    "start": "node --env-file-if-exists=../../.env --import tsx src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@spendlio/ai": "workspace:*",
    "@spendlio/contracts": "workspace:*",
    "@spendlio/core": "workspace:*",
    "@spendlio/db": "workspace:*",
    "@spendlio/queue": "workspace:*",
    "@spendlio/storage": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^2.1.9"
  }
}
