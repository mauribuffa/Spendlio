# PROGRESS — Spendlio build tracker

> **Single source of truth for "where are we."** Claude Code updates this after **every** completed step: tick the box, set the status line, and add a Build-Log entry. Humans skim the top; details live in the log.

**Current status:** _Phases 1–4 COMPLETE and PROVEN END-TO-END. Full stack runs live: web (Next.js + `@spendlio/ui`) → API (NestJS) → Postgres + Redis + MinIO, with `apps/worker` (BullMQ) and the AI assistant (offline default / Vercel AI SDK live). Built by a 4-specialist agent team. **Phase 5 (Auth.js) is DONE (ADR-033)** — email OTP + short-lived JWT to the API replaced the dev `x-user-id` AuthGuard; verified live (dev + OTP sign-in, provisioning, 401 on missing/invalid token). The web app + `@spendlio/ui` were since **reconciled to the canonical Claude Design bundle** (real token vocabulary, bundle-accurate chrome + pages, Modal/onboarding) — presentation-only, data flow untouched (ADR-023). Most recently, **Split Groups + per-person Remind shipped** (NestJS `groups` resource + synchronous `POST /people/:id/remind`; ADR-025), the **`@spendlio/db` seed was enriched** to populate every page (accounts/transactions/budgets/people/groups/split/settlement), and an **explicit self-person** (`is_self`, hidden from `GET /people`) now backs model-B split balances (ADR-024, migration `0001`). 123 unit/integration tests green (ui 45) + a live e2e gate (create→categorize→split→balances→receipt→ocr→recap→AI chat) + a web render smoke; whole monorepo typecheck + `next build` green; new endpoints verified live (groups list/create+ownership, remind ok/404, self-person excluded, balances correct)._
>
> _**2026-06-16 — money/security/queue hardening (ADR-028/029/030).** A 7-phase pass fixed the silently-wrong split balances (user-created splits now route through the self-person server-side; both balance readers are self-aware), closed IDOR gaps (transaction `accountId`, split participants, presign query) + a pagination 500, hardened the queue (enqueue-time validation, DB-backed idempotency, fixed the dead OCR→categorize chain, bounded provider timeouts) and added a **real dead-letter queue** (durable `dead_letters` + redrive), then cleaned up architecture (recap/recurring math → `@spendlio/core`; shared `PresignedUploadSchema`; thin Me/Recaps services), web duplication (shared `formatWhole`/`categoryColor`; currency-aware Add-expense; real assistant name), and UI a11y (Tag/Select/Modal) + storage fail-fast. Migrations `0003` (self-person uniqueness) + `0004` (notify dedupe, recurring unique, `dead_letters`). Package unit suites green; **DB/Redis-gated integration + live e2e to be re-run with the local stack up** (no docker daemon in the sandbox where this landed)._
>
> _**2026-06-16 — front-end responsive overhaul + feature-based restructure (ADR-031/032), merged to `main`.** A 7-phase pass: a token-driven `.spl-*` responsive utility layer + `--bp-md` added to `@spendlio/ui/styles.css` (the documented home for what inline style can't express); `apps/web` reorganised feature-based (`app/` routing-only, shared `components/{layout,form,feedback,domain,expenses}`, `features/<domain>/{components,lib}`); the app made **mobile-responsive end to end** — app shell collapses to a hamburger drawer + scrim < 768px, every page grid collapses to one column, the transactions table swaps to a `TransactionRow` card list, modals stay centered and sized to fit small screens, the page header stacks; shared `FormField`/`FieldError` extracted; the missing `loading`/`error`/`not-found` route files added and a `ToastProvider` wired; all 57 component files renamed PascalCase → kebab-case (exports unchanged, history preserved via two-step `git mv`). **Verified live** at 390/768/1280px (Playwright — zero horizontal overflow); full typecheck + 45 ui tests + `next build` green on `main`. Deferred: a few minor UX polish items + the `unicorn/filename-case` lint rule (no package has a `lint` script yet, so it would be inert)._
>
> _**2026-06-18 — assistant capability expansion + injection hardening (ADR-041).** The read-only tool-calling assistant grew from 4 → **9 tools** (added `searchTransactions`, `spendingTrend`, `balanceWithPerson`, `monthlyRecap`, `accountBalances`) — all `user_id`-scoped, returning exact integer cents (the model never does money math). Free-text search is **lexical `ILIKE` + LLM query-expansion** (no pgvector/BM25/FTS — the per-user corpus is tiny). A pragmatic injection-hardening pass landed alongside: chat-contract input caps, system-prompt spotlighting (tool output + stored text = DATA, not instructions), a plain-text web render guard test, and a per-user Redis rate limit (30/min); read-only itself is the security boundary (no mutations + structural tenant isolation). No migration. **Proven here:** `pnpm -r typecheck` 11/11 (api still fast — live module lazy-loaded), `@spendlio/ai` unit tests 44 passed/10 skipped, the **DB integration suite 54 passed/0 skipped** against the local Postgres (all 10 new-tool cases), and `pnpm --filter web build` green. **Still a manual gate** (needs a running stack + a live Anthropic/OpenAI key): real LLM tool-selection citing exact cents, the >30/min → 429 check, and the injection-note test._
**Last updated:** _2026-06-18_

---

## Phase 1 · Foundation
- [x] 01 · Monorepo skeleton (pnpm + Turborepo + `packages/config`) — `docs/build/01-monorepo-skeleton.md`
- [x] 02 · Docker & env (Postgres + Redis + MinIO + `.env`) — `docs/build/02-docker-and-env.md`
- [x] 03 · `@spendlio/contracts` (Zod) — `docs/build/03-contracts-package.md`
- [x] 04 · `@spendlio/db` (Drizzle schema + migrations + seed) — `docs/build/04-db-package-drizzle.md` — 16 tables, migration applied, 12 categories + demo user seeded
- [x] 05 · `@spendlio/core` (money + split engine + tests) — `docs/build/05-core-package.md` — 24 tests; every split sums exactly
- [x] ✅ Phase 1 acceptance — `docs/build/99-acceptance.md` — verified by integration gate (migrate + seed + typecheck + core tests)

## Phase 2 · API (NestJS) — ✅ complete (`apps/api`, proven end-to-end)
- [x] 06 · API foundation + `transactions` resource — `docs/build/06-api-nestjs.md`
- [x] `budgets` resource (+ `GET /budgets/status` via core)
- [x] `accounts` resource
- [x] `categories` resource
- [x] `splits` + `balances` (via core)
- [x] `receipts` (presign → MinIO upload; enqueues `ocr`)
- [x] `assistant` (POST `/assistant`, streamed) + `/me`, `/people`, `/recaps/:month`

## Phase 3 · Web (Next.js) — ✅ complete (`apps/web`, renders live API data)
- [x] App Router skeleton + `@spendlio/ui` wired (`styles.css` + 3 fonts via `next/font`)
- [x] Overview, Transactions, Budgets, Split, Insights (+Assistant chat), Settings — all render live data; `next build` green

## Phase 4 · Queue + workers + storage — ✅ complete
- [x] `packages/queue` (BullMQ, typed `enqueue`, idempotent hyphen jobIds) + `packages/storage` (S3/MinIO `BlobStore`, presigned PUT)
- [x] `apps/worker` (separate app) — ocr → categorize, recurring, recap, notify (live-proven, idempotent)
- [x] `@spendlio/ai` — provider interface (offline default + Vercel AI SDK live), categorization, OCR, streaming tool-calling assistant

## Phase 5 · Auth — ✅ done (2026-06-16) · email OTP via Auth.js + short-lived JWT to the API (ADR-033)
- [x] Auth.js (web, email OTP, JWT sessions) + short-lived Bearer JWT to the API; dev `AuthGuard` (`x-user-id`) removed

## Next up — candidate next steps (post–Phase 5)
Phase 5 unblocked "real users / deploy." Each is a brainstorm → spec → plan candidate:
- [x] **A) Real email delivery for OTP** — ✅ DONE (ADR-036). Vendor-neutral `SmtpEmailSender` (nodemailer) behind the existing `EmailSender` interface, selected when `SMTP_HOST` is set (else the dev console sender); **Mailpit** added to `docker-compose.yml` for local "$0" verification; send-failure rolls back the Redis keys + returns 503 (immediate retry). Verified live (Mailpit inbox→verify, console fallback, dead-host 503). Auth is now real-user-ready.
- [ ] **B) Deploy mini-phase** — resolve hosting (ADR-013); set real `AUTH_SECRET` + `API_JWT_SECRET` + `SMTP_*` in prod (drop the `change-me-dev-only` fallback — add a startup fail-fast in `auth.guard.ts` + `lib/auth-token.ts`); rename `apps/web/middleware.ts` → `proxy.ts` (Next 16 deprecation). **A is now done, so B is unblocked.**
- [~] **C) Remaining feature gaps** — real onboarding persistence ✅ DONE (ADR-038): currency + language now persist to the user + an `onboarded_at` gate (root-layout interstitial). Still open: FX/multi-currency totals (ADR-016).

---

## Open decisions to resolve (from `docs/learning/decisions.md`)
- [x] ADR-011 · AI/OCR provider + privacy posture — ✅ RESOLVED (Vercel AI SDK; offline default + Anthropic/OpenAI live; see ADR-019)
- [ ] ADR-013 · final hosting target (at deploy)
- [ ] ADR-016 · FX rates provider + rounding rule (when building multi-currency totals)

## Follow-ups (tracked, non-blocking — runtime is proven)
- [x] 🐛 **Settlement balances diverge (Split page vs AI assistant)** — ✅ FIXED (ADR-040). Settlements are now you↔friend + a direction (`CreateSettlementInput = { personId, direction, amount, currency }`, self resolved server-side); ONE canonical `@spendlio/core` `netBalances()` is the single balance source both `SplitsService.balances()` and `ai.netBalances()` delegate to (so they can't diverge); settle-up form is one friend + a direction toggle. TDD + live e2e (both directions) + 3-lens adversarial review.
- [x] #11 · ✅ DONE (ADR-030) — `computeRecap` + recurring cadence math lifted from `apps/worker/src/lib` into `@spendlio/core` with unit tests; the worker imports from `@spendlio/core`.
- [x] #12 · ✅ DONE — `@spendlio/ai` live module lazy-loaded behind a non-literal dynamic import; `apps/api tsc --noEmit` 303s+OOM → **1.76s** clean (ADR-022). Runtime unchanged.
- [x] Optional: **real dead-letter queue + alerting** — ✅ DONE (ADR-029): durable `dead_letters` table written on a job's final failed attempt + `alertFailure()` sink + `redriveDeadLetter()` helper. `pgvector` for semantic search still not needed.
- [ ] 🧪 **Assistant live-LLM gate (ADR-041)** — the offline path + DB-tool layer are proven (54/54 integration green), but three paths need a running api+worker+web stack + a real Anthropic/OpenAI key: the LLM actually selecting search/trend/per-person/recap and citing exact cents, the >30/min → HTTP 429 trip, and the injection-note test (a txn note `IGNORE PREVIOUS INSTRUCTIONS…` reported as data, not obeyed). Run with the local stack up.
- [ ] Optional: **`pg_trgm` fuzzy search (ADR-041)** — assistant search is lexical `ILIKE` + LLM query-expansion today; add `pg_trgm` only if fuzzy/typo matching proves needed. No new table/migration unless adopted.

## Design implementation — status & next steps (ADR-023)
**Done:** canonical token system + 20 components reconciled; chrome (sidebar, sticky topbar) + Donut/BarChart; reskinned Overview, Transactions, Accounts, Budgets, Split, Settings; **separate Assistant (`/assistant`, chat) + Insights (`/insights`, AI cards + donut)**; presentational `/onboarding`; reusable `Modal`; rich **Add-expense modal** (amount/description/category picker + Evenly/Exact/% split) and **Scan** modal in the topbar, wired to real endpoints. Post-merge fixes: Modal portals to `<body>` (was clipping under the blurred topbar); removed the duplicate Transactions add button.

**Known gaps / next steps (need a decision or backend work):**
- [x] **Split "Groups" + per-person "Remind"** (design Image #7) — ✅ DONE (ADR-025). NestJS `groups` resource (`GET`/`POST /groups`, member-ownership validated) + synchronous `POST /people/:id/remind` (records an in-app `settle_reminder` notification; no email/push transport yet). Split page reskinned to the bundle: Groups card (AvatarGroup + net badge derived from balances) + People card (inline Remind for "owes you") + New-group form. Group net is web-derived (sum of members' balances), not stored.
- [x] **Split math vs the user** — ✅ DONE (ADR-028, builds on ADR-024's self-person). User-created splits route through the self-person **server-side**: `CreateSplitInput` drops the client `payerId`, `total` is the full expense, `SplitsService.create` resolves the self-person (race-safe partial unique index), validates participant ownership (the missing IDOR check), injects + stores the self share, and **both** balance readers (`SplitsService.balances` + the AI `netBalances`) skip the self share — so no friend's debt vanishes and the assistant no longer reports a phantom "You". `core` no longer drops a remainder cent when the payer isn't a participant. Migration `0003`.
- [x] **Seed data is sparse** — ✅ DONE. `@spendlio/db` seed now has 4 accounts (real balances via txns), 23 transactions across June+May 2026 with `accountId` + varied statuses (income/recurring/split/pending/cleared), 5 budgets, 4 friends + a self-person, 3 groups, 2 splits (+shares), and a settlement. Idempotent (fixed UUIDs + `onConflictDoNothing`). Verified live: Overview/Transactions/Accounts/Split populate; Alex/Sam owe $137.00, Maya settled. Mockup figures ($8,186 etc.) remain illustrative.
- [x] **Onboarding is presentational** — ✅ DONE (ADR-038). Currency + language persist to `users` and a nullable `onboarded_at` marker (migration `0006`) gates the flow as a root-layout interstitial (unskippable, any route). New `POST /me/onboarding` endpoint; standalone `/onboarding` route removed.
- [ ] **Mobile UI kit** — out of scope until an `apps/mobile` exists; the bundle's mobile screens are archived at `docs/design-bundle/ui_kits/mobile/`.
- [~] **Live visual QA** — extensively done on 2026-06-16 via Playwright: responsive sweep at 390/768/1280px (shell drawer, grids, transactions card list, centered modal — caught a scrim + a modal-centering bug), a console-error sweep of all 12 pages (caught hydration + duplicate-key bugs), and an end-to-end write-path smoke of all 6 mutation flows (success toasts confirmed). The **receipt upload→OCR→review→confirm flow is now exercised live (2026-06-17)** against the OpenAI provider — including retry-on-failure and the OCR-suggested category pre-fill. **Still not exercised live:** `error.tsx` / `loading.tsx` (hard to trigger without a forced error / slow fetch).

---

## Build log
Newest first. One row per completed step or notable decision.

| Date | Step / change | Notes (what was done, anything surprising, ADRs added) |
|------|---------------|--------------------------------------------------------|
| 2026-06-18 | Assistant expansion + injection hardening (ADR-041) | 5 new read tools (`searchTransactions`/`spendingTrend`/`balanceWithPerson`/`monthlyRecap`/`accountBalances` → 9 total), all `user_id`-scoped + exact integer cents (model never does money math). Search is lexical `ILIKE` over title/merchant/note + LLM query-expansion (no pgvector/BM25/FTS — per-user corpus is tiny; `pg_trgm` is the documented follow-up). Hardening: chat input caps on the contract, system-prompt spotlighting (tool output + stored text = DATA, not instructions), plain-text web render guard test, per-user Redis rate limit (30/min). `accountBalances` is per-account + per-currency only (no FX rollup; ADR-016 open). **No migration.** **Verified (runnable here):** `pnpm -r typecheck` 11/11 Done (api stays fast — live module still lazy-loaded); `@spendlio/ai` unit tests 44 passed / 10 skipped (integration DB-gated); **DB integration suite** (`DATABASE_URL` set, local Postgres up) **54 passed / 0 skipped** — all 10 new-tool integration cases green; `pnpm --filter web build` green (16 routes, only the known middleware→proxy deprecation warning = Next up B). **STILL TO BE EXERCISED MANUALLY (live-LLM gate, needs a running api+worker+web stack + a real Anthropic/OpenAI key):** the LLM actually selecting search/trend/per-person/recap and citing exact cents, the >30/min → HTTP 429 check, and the injection-note test (a txn note `IGNORE PREVIOUS INSTRUCTIONS…` reported as data, not obeyed). The offline provider path and the DB-tool layer are proven; the live tool-selection / 429 / injection-note paths are not yet driven. |
| 2026-06-18 | Fix · model-B settlements + canonical balance fn (ADR-040) | Fixed the latent bug found during the cleanup: the Split page and the AI assistant computed settlement balances differently, and "a friend paid me" couldn't be expressed (the settle-up form picked two friends; self is excluded from `GET /people`). Model B = a settlement is you↔one friend + a direction. `CreateSettlementInput` → `{ personId, direction: they_paid_you\|you_paid_them, amount, currency }`; `SettlementsService.create` resolves the self-person server-side (new shared `common/self-person.ts`, mirroring split payer resolution) and writes from/to — no migration. New canonical **`@spendlio/core` `netBalances(shares, settlements, selfId)`** (replaces the dead edge-based `netForUser`) is the SINGLE balance source; both `SplitsService.balances()` and `@spendlio/ai`'s `netBalances()` delegate to it → they can't diverge. Settle-up form is now one friend + a direction toggle. **TDD** core (both directions), typecheck 10/10 + all suites green, **live e2e** on a throwaway user (even split → friend owes 5000; they_paid_you 5000 → cleared; you_paid_them 3000 → +3000; old `{from,to}` shape → 400), 3-lens adversarial review (all clean). Test data cleaned up via DB (cascade was blocked by settlements→people RESTRICT, so deleted in dependency order). |
| 2026-06-18 | Codebase review + cleanup pass (5 commits, adversarially verified) | A whole-repo review (4 parallel reviewers) → a phased cleanup, each phase gated (typecheck/tests/build) + adversarially verified before commit. **Shipped:** (1) `core` split guards (reject negative/duplicate shares) + `NotifyJob.type` enum + dropped 2 redundant DB indexes (migration `0007`); (2) **API typed end-to-end** — `db: any` → `db: DB` across all 13 providers (surfaced + fixed latent varchar→enum narrowings), new `or404()` helper dedups the not-found guard, **budgets `status()` N+1 → one bounded query** (proven byte-equivalent); (3) web DRY — shared `summarizeSpending()` for Overview+Insights, local `cap` → `@spendlio/ui` `capitalize`; (4) DS polish — tokenized the avatar ramp + settings-form colors (they duplicated `--cat-*`) + `#fff`→`--neutral-0`, exact-match font sizes → scale tokens, small a11y (Select keys, ProgressBar `aria-valuetext`) — **zero visual change** (every swap byte-identical); (5b) dropped Badge's dead `primary` tone alias. **Decided NOT to do (ADR-039):** decouple `@spendlio/ui` from `@spendlio/contracts` — contracts is the canonical source of truth and golden rule #1 has deps point *toward* it, so `ui→contracts` is intentional; domain composites stay in the tested `ui` package. **Found + deferred (needs a product decision):** the settlement-balances divergence bug (see follow-ups + decisions.md). The `balances()`↔`ai.netBalances()` core unification was deferred for the same reason. No behavior changed in any shipped commit. |
| 2026-06-18 | Next up · C — Real onboarding persistence (ADR-038) | The onboarding flow was presentational (collected currency + language, then `router.push('/')`, persisting/gating nothing). Now: the two choices persist to the existing `users.default_currency` + `locale`, plus a new nullable **`onboarded_at timestamptz`** marker (migration `0006`, rule #7) that gates the flow. Dedicated **`POST /me/onboarding`** (`CompleteOnboardingInput = UserSchema.pick({ defaultCurrency, locale })`) sets prefs + stamps `onboarded_at` atomically — kept off `PATCH /me` so Settings stays minimal and the client can't set the marker. **Gate = root-layout interstitial** (chosen over a redirect via a 2-option decision): the server layout reads `getMe()` and renders the onboarding flow instead of `AppShell` for a signed-in user with null `onboardedAt`, on **any** route (unskippable, no deep-link leak, no pathname detection); completion `router.refresh()`es to re-run the gate. Standalone `/onboarding` route deleted (route list confirms); onboarding flow moved to `features/onboarding/{components,lib}`; pre-auth "I already have an account" link dropped (now post-auth); `/me`-unreachable falls through to the shell (no trap); seed marks the demo user pre-onboarded. **Verify:** 4 new contracts unit tests (33 green incl. live GET-guard once stack synced), `pnpm typecheck` 10/10, `@spendlio/web build` green, migration `0006` applied; **live** (demo Bearer): `GET /me` null → `POST /me/onboarding {EUR,es-AR}` 200 → `onboardedAt` stamped + prefs persisted, bad currency → 400 (demo restored to USD/en-US after). NB: browser e2e of the visual interstitial not driven; locale *consumption* (UI i18n) out of scope — value is persisted only. |
| 2026-06-18 | Security · receipt `imageKey` namespace guard (ADR-037) | Closed a cross-tenant IDOR in receipt register: `POST /receipts` trusted the client-asserted `imageKey` (`CreateReceiptInput.imageKey` was only `min(1)`) and stored it, while `GET /receipts/:id/image-url` presign-**downloads** `row.imageKey` — so a crafted `imageKey` pointing at `receipts/<otherUserId>/<hash>` would let an attacker read another user's blob (latent: keys embed an unguessable userId + sha256/uuid, but the trust gap was real; `presign` derives the key server-side yet `register` re-asserted it). Fix: new pure `isOwnReceiptKey(key, userId, sha256?)` in `@spendlio/storage` (co-located with `receiptKey`), enforced in `ReceiptsService.create()` before any DB write → **400**. Rule = single object directly under `receipts/<userId>/`, no nested/`..` paths, and basename must name the declared sha256. **TDD:** 7 new storage unit tests written red-first then green (15/15); storage+api typecheck green; **live**: foreign-prefix/traversal/outside-root register → 400, valid token still lists 200, zero DB writes (rejected pre-insert). Found while reviewing the MinIO key-path logic; sibling to ADR-028 (which hardened the presign *query*, not the register *key*). |
| 2026-06-17 | Next up · A — Real OTP email over SMTP (ADR-036) | Resolved ADR-033's deferred prod email vendor. New **`SmtpEmailSender`** (nodemailer) wired behind the existing `EmailSender` interface; `AuthModule` now selects it via `useFactory` when `SMTP_HOST` is set, else the dev `ConsoleEmailSender` (mirrors `@spendlio/ai`'s offline/live env-gating). Vendor-neutral SMTP keeps local↔prod a config swap (golden rule #6) — **Mailpit** added to `docker-compose.yml` (`:1025` SMTP / `:8025` inbox) so the path is verifiable locally at $0; prod swaps `SMTP_*` creds. Bounded nodemailer timeouts (~10s). **`OtpService.request()` hardened**: on send failure it now rolls back the Redis code + cooldown keys and throws **503** (was silently 200 with an email that never arrives), so the user can retry immediately without leaking enumeration. `.env.example` gets the `SMTP_*` block; `nodemailer` + `@types/nodemailer` added to `apps/api`. `pnpm --filter @spendlio/api typecheck` + `@spendlio/contracts` tests green; **verified live** (full stack + Mailpit): request→inbox→verify provisions; `SMTP_HOST` unset→console fallback; dead SMTP port→503 + 2nd request also 503 (cooldown cleared). **Adversarial review (3-lens workflow) caught 2 real issues, both fixed + re-verified:** (1) MEDIUM — `/request` echoed `devCode` gated on `NODE_ENV` not the transport, so a non-prod/staging env with a real `SMTP_HOST` would have BOTH emailed the code AND returned it in the unauthenticated response (OTP bypass); now gated on a new `EmailSender.exposesCode` flag (console=true, SMTP=false) → SMTP withholds `devCode`, console still echoes it (both re-verified live). (2) LOW — `secure` couldn't derive from the port, so `SMTP_PORT=465` without `SMTP_SECURE=true` stalls to a timeout; now `secure` defaults from the port (465⇒TLS) when `SMTP_SECURE` is unset. No API unit tests per the locked surgical policy. Spec: `docs/superpowers/specs/2026-06-17-real-otp-email-smtp-design.md`. NB: stale dev API on :4000 was restarted onto the new code; test user rows removed post-verify. |
| 2026-06-17 | Tidy · flaky queue test + tracker + cleanup | Fixed the flaky `@spendlio/queue` Redis test: it asserted `getJobCounts('wait') >= 1` right after enqueue, which raced a running worker draining the queue → now asserts the job exists via `getJob(jobId)` (5/5 stable). Marked the receipt OCR live-flow QA gap exercised (above) + logged the three receipt features below. Cleaned test leftovers from the DB (1 orphan OCR expense whose receipt was deleted + 6 `ocr` dead_letters). |
| 2026-06-17 | Receipts · OCR-suggested category (ADR-035) | OCR vision model now infers a spending `CategoryKey` (`ReceiptOcrResult.category`, **`.nullable()`** so it stays OpenAI strict-output-safe; rides the `ocr` JSONB — no migration), surfaced via `ReceiptSchema.category` to **pre-fill** the review form. `receipts.confirm()` no longer enqueues the `categorize` job, so the user's reviewed category is **authoritative** (job untouched for manual/import/recurring). ai + contracts tests + full typecheck green; **verified live** (category pre-fills, sticks through approve). |
| 2026-06-17 | Receipts · OCR OpenAI strict-schema fix | Every live OpenAI scan was failing: `generateObject` uses **strict** structured outputs (every property must be in `required`), but `ReceiptLineItem.quantity` had `.default()` → emitted as optional → OpenAI rejected ("Missing 'quantity'"). Fixed with an OCR-specific line item that makes `quantity` required (`ReceiptLineItem.extend`), keeping the contract's default for the confirm/storage paths. Failing→passing schema test added; **verified live** (scans parse). NB: tool/function-calling is sent non-strict by default, so the assistant tools' `.default()` params are unaffected (breadcrumb added on `recentTransactions`). |
| 2026-06-17 | Receipts · retry + failure reason (ADR-034) | Failed scans now show a **friendly reason** + a **Retry** button (list + detail). Worker classifies the raw error → a `ReceiptFailureReason` code (`@spendlio/core` `classifyOcrFailure`) stored on a new `receipts.failure_reason` column (migration **`0005`**); the web maps code→text (raw stays in `dead_letters`). `POST /receipts/:id/retry` (owner-scoped, failed-only) flips to `processing` + re-runs OCR via a new `queue.requeue()` that **removes the deduped job before re-enqueue** (BullMQ ignores a re-add of an existing jobId; `removeOnFail` keeps the failed job). contracts + core tests + full typecheck green; multi-agent two-stage review (caught a missing soft-delete guard + token/markup nits). |
| 2026-06-16 | Phase 5 · Auth — email OTP + Auth.js + JWT | Replaced the dev `x-user-id` trust path with real auth (**ADR-033**). Web: Auth.js (next-auth v5, JWT sessions, no adapter) with an **email-OTP** Credentials provider + a dev-only demo login; `middleware.ts` gates routes; `lib/auth-token.ts#getApiToken()` mints a short-lived **HS256 Bearer JWT** now sent by `lib/api.ts` + the assistant proxy. API: public `auth/otp/{request,verify}` (codes **HMAC'd in Redis** via `getRedisClient()`, 10-min TTL, 5 attempts, 60s cooldown, no enumeration), provision-by-email (**no migration**), and `AuthGuard` rewritten to verify the JWT (`jose`). Contracts: OTP/auth schemas; live test now authenticates via a minted Bearer + covers 401/OTP paths. Docs: ADR-033, `08-auth-security.md` locked, `05`/`06`/`07`/`glossary`/`README` updated, build step `07-auth.md`. Deferred: prod email vendor, refresh tokens/mobile, OAuth, MFA, RLS. |
| 2026-06-16 | QA hardening pass — read + client-runtime + write paths | Live QA sweep after the front-end overhaul; 3 pre-existing bugs found + fixed (all pushed). **Read path:** validated all 16 GET endpoints against the web's exact schemas → a `{items}` pagination-envelope drift in 5 resources (categories/people/accounts/budgets/splits) was parsed as a bare array, so the Zod parse threw, `safe()` swallowed it, and the UI silently got `[]` (empty receipt categories, empty People/Split lists, `—` account labels). Fixed with a shared `listItems()` unwrap; added a self-skipping live contract guard (`packages/contracts/src/api-contract.live.test.ts`). **Client runtime:** console-swept all 12 pages → fixed an app-wide **hydration mismatch** (`ToastProvider` gated its portal on `typeof document`, differing server vs first client render → mount-gated it) and **duplicate React keys** (budgets keyed by `category`, but `BudgetStatus` has no id and the data holds two `dining` budgets). **Write path:** smoke-tested all 6 mutation flows end-to-end via the UI (add person/expense/group, settle up, update settings, remind) — all pass; test rows created then removed via SQL id-diff, DB restored to baseline. **Not covered:** the receipt upload→OCR→review→confirm flow (needs a real image + OCR provider). typecheck + `next build` + ui 45 + contracts 15 green. |
| 2026-06-16 | Front-end responsive overhaul + feature-based restructure (ADR-031/032) | 7-phase pass, merged to `main`. **Foundation:** `.spl-*` responsive utility layer (`spl-cards`/`spl-grid-asym`/`spl-form-row`/`spl-stack`/visibility) + `--bp-md` in `@spendlio/ui` (per-instance values via inline `--spl-cols`; `@media` can't read CSS vars so 768px is literal). **Structure:** `apps/web` reorganised — `app/` routing-only, shared `components/`, `features/<domain>/{components,lib}`; all local imports on the `@/` alias; shared `FormField`/`FieldError` (killed 3 duplicated label/error copies). **Responsive:** AppShell → hamburger drawer + scrim < 768px (only the toggle is JS); page grids → single column; transactions `<table>` → mobile `TransactionRow` card list via CSS-only `.spl-only-*` swap; Modal stays centered at every size (tighter margins on small screens — an initial bottom-sheet variant left a 480px slab glued to the bottom at 480–767px, so it was reverted to centered); PageHeader stacks (fixed a title/button collision). **Best practice:** `loading`/`error`/`not-found` route files; `ToastProvider` + success toasts (the `Toast` primitive was previously unused). **Naming:** all 57 component files → kebab-case (two-step `git mv`, exports unchanged). **Verified live** via Playwright at 390/768/1280px (zero horizontal overflow) + full typecheck + ui 45 + `next build` green. Bugs caught in visual QA: transparent scrim from invalid `rgba(var(--shadow-color),…)` → fixed to `--surface-overlay`. Deferred: minor UX polish (currency symbol in `AmountInput`, destructive-confirm dialogs, chart aria) + `unicorn/filename-case` (inert until a `lint` script exists). |
| 2026-06-16 | Phase 7 · Docs/ADRs/trackers | Added **ADR-028** (route user-created splits through the self-person — resolves the ADR-021/024 follow-up), **ADR-029** (queue hardening + real dead-letter queue), **ADR-030** (contracts own the presign shape; core lift; thin Me/Recaps services). Marked ADR-024's deferred modal-split-create path resolved. Corrected the `@spendlio/config` claim in `CLAUDE.md` + `01-monorepo.md` (config ships **ESLint only**; base tsconfig is `tsconfig.base.json` at the repo root; Prettier on defaults). Updated `07-queues-jobs.md` to describe the real DLQ (the BullMQ `failed` set is evictable; `dead_letters` is the durable record). Ticked follow-up #11 + the DLQ optional + the split-math gap above. |
| 2026-06-16 | Phase 6 · UI a11y + fail-fast | **Tag** removable chip → non-interactive container with a real nested remove `<button>` (no `role=button` span inside a `<button>`); CSS resets the button's UA styles + focus ring. **Select** associates label/control via `useId` (`htmlFor`/`id`, with an `id` prop override). **Modal** gains `aria-labelledby`/`aria-label`, focus-on-open, focus-restore-on-close, and a Tab focus trap. **storage** `S3BlobStore` throws a clear error when `STORAGE_KEY`/`STORAGE_SECRET` are absent (was building a client with empty creds). contracts documents the bare value-object vs `XSchema` convention. `@spendlio/ai` barrel drops an unused import + redundant re-export. ui 45 / storage 7 / ai 18 / contracts 14 tests green; typecheck green. |
| 2026-06-16 | Phase 5 · Web dedupe + currency + assistant name | Extracted the verbatim-copied `whole()` formatter + `CAT_SLOT`/`catColor` + `cap` from the home/insights/budgets pages into `@spendlio/ui` (`formatWhole`, `capitalize`, `categoryColor` — the latter from `CategoryIcon`'s canonical map). **AddExpenseModal** routes amounts through `toMinorUnits`/`getCurrencyDecimals`, **defaults to the user's currency** (loaded via a new `loadDefaultCurrency` server action, matching the existing `loadPeople` pattern — no prop-threading), formats with `formatMoney`; the people-load effect is guarded by a `loaded` flag. **Assistant** shows the signed-in user's name (page fetches `getMe`) instead of a hardcoded "Alex Rivera". **Deferred:** Avatar `PALETTE` → CSS vars (the hex was deliberately chosen for white-text contrast; unverifiable without a render pass) and the onboarding route group (requires relocating every route under a group — high churn, low payoff). ui 45 + web typecheck green. |
| 2026-06-16 | Phase 4 · Architecture cleanups | Lifted `computeRecap` + recurring cadence math from `apps/worker/src/lib` into `@spendlio/core` with unit tests (core 49 tests); worker imports from core. Promoted `PresignedUploadSchema` to `@spendlio/contracts` (consumed by `@spendlio/storage` + the web edge — deletes the hand-copied web mirror). Pointed the live receipt `generateObject` at `ReceiptOcrResult` instead of re-inlining the shape. Extracted `MeService`/`RecapsService` so those controllers stay thin. **Deferred** (noted in ADR-030): moving the assistant tool-row types wholesale into contracts (AI seam surface; divergent `CategorySpend` naming). Whole-monorepo typecheck green. |
| 2026-06-16 | Phase 3 · Queue hardening + real DLQ (ADR-029) | `enqueue()` validates the payload against the contracts job schema at the producer. DB-backed idempotency: `notify` `dedupeKey` + partial unique index, `recurring` partial unique index `(recurring_id, occurred_at)`, both `onConflictDoNothing`. `RecapJob.month` tightened to `YYYY-MM` + invalid-range guard. Fixed the dead `ocr→categorize` chain (now enqueued from `receipts.confirm()`; OCR early-returns on `parsed` so retries don't re-call the paid provider). Bounded provider timeouts (`AbortSignal.timeout`) + OCR-worker `lockDuration`. **Real dead-letter queue**: `dead_letters` table (migration `0004`) written on the final failed attempt + `alertFailure()` sink + `redriveDeadLetter()`. queue validation + core + ai + contracts tests green; **DB/Redis-gated worker/integration tests + live e2e not run here (no docker daemon in the planning sandbox)**. |
| 2026-06-16 | Phase 2 · Security: IDOR & unvalidated inputs | Transactions verify a supplied `accountId` belongs to the user on create/update (was attachable to another user's account). Receipt presign query validated via a new `PresignReceiptQuery` (`contentType` ∈ allowed mime types, `sha256` hex) so an attacker can't shape the storage key. Malformed pagination `?cursor=` now degrades to "no cursor" instead of 500ing. api + contracts typecheck green (no API test runner exists; covered by typecheck + the live e2e gate). |
| 2026-06-16 | Phase 1 · Route splits through the self-person (ADR-028) | **The fix for the silently-wrong balances.** `core`: split remainder distributed only over real share-holders (no dropped cent when the payer isn't a participant) + assert `Σshares===total` + percent epsilon. `contracts`: `CreateSplitInput` drops client `payerId`, adds a mode-aware weight refine. `api`: `SplitsService.create` resolves/lazy-creates the self-person (race-safe partial unique index, migration `0003`), validates participant ownership (the missing IDOR check), sets `payerId=self`, injects + stores the self share, strips self from `get()`. `ai`: `netBalances` made self-aware (skips the self share, drops the phantom "You" that leaked from the seed). `web`: sends the **full expense** as `total`, no `payerId`. core 40→ (split cases) + ai 18 + contracts 14 green; **live e2e/integration deferred (no docker here)**. |
| 2026-06-15 | HEIC → JPEG on receipt upload | iPhone HEIC photos broke twice: OCR (OpenAI vision accepts only PNG/JPEG/WEBP/GIF) and the preview (`<img>` — only Safari renders HEIC). `UploadReceipt` now detects HEIC/HEIF (by `file.type` or `.heic/.heif` name) and converts to JPEG client-side via **`heic2any`** (libheif WASM) before hashing/upload — **lazy-imported** only when a HEIC is picked so the ~MB decoder stays out of the main bundle. Conversion must be client-side (bytes go direct to storage; the API never sees them). Added a Converting…/Uploading… button state; `accept="image/*,.heic,.heif"`. Verified by typecheck + `next build`; **runtime conversion is browser-only (WASM) so it needs a manual QA pass with a real iPhone photo** — not exercised by curl. |
| 2026-06-15 | Per-currency minor units in manual-entry actions | Replaced the hardcoded `Math.round(major * 100)` in `createTransactionAction`, `createExpenseAction`, and `settleUpAction` with `toMinorUnits(major, currency)` — correct for 0-decimal (JPY/CLP) and 3-decimal (BHD) currencies, identical for 2-decimal ones. No-op for the currencies currently offered (all 2-dp) but future-proofs adding others. Add-expense modal stays USD-only (no currency picker; its client `×100` is fine there). web typecheck + `next build` green. Closes the ADR-027 follow-up. |
| 2026-06-15 | Receipt review & approve → expense (ADR-027) | OCR is now a **suggestion the user approves**, not auto-committed money (motivated by ARS `1.500,00` misreads). After scan the web routes to `/receipts/<id>`; once `parsed && !transactionId` the detail page shows an editable **review form** (merchant/date/total/currency, add/remove/edit line items + "auto-sum", required category). Approve → new **`POST /receipts/:id/confirm`** (`ConfirmReceiptInput`) writes corrected values back to the receipt **and** creates the linked expense (`source:'ocr'`, `receiptId`), sets `transactionId`; re-confirm → 400; page then shows "Added to expenses ✓". **Money:** the action converts MAJOR→minor via per-currency `toMinorUnits` (ARS=2/JPY=0/BHD=3), replacing the hardcoded ×100 for this path (the other manual-entry actions' ×100 is a noted follow-up). No status-enum change (`transactionId` = converted). New `ReceiptReviewForm`; `registerReceiptAction` returns the id so the uploader can navigate. **Verified live:** parsed→confirm created `-150000` ARS groceries expense linked to the receipt, re-confirm 400, `toMinorUnits` scales ARS/JPY/USD/BHD right. typecheck + contracts 14 tests + `next build` green. Spec + ADR-027 added. Closes the previously-omitted "create transaction from receipt". |
| 2026-06-15 | Receipt UI fixes (live QA findings) | Three pre-existing receipts gaps surfaced once the OCR flow actually ran end-to-end: (1) **Scan modal didn't close** — `UploadReceipt` now takes an optional `onSuccess`; `TopbarActions` passes it to close the Scan modal after a successful upload. (2) **Line items missing** — the worker stores OCR collapsed in the `ocr` JSONB column, but the page reads `receipt.lineItems` (which Zod defaulted to `[]`); the receipts service now reshapes the row → contract (`lineItems` from `ocr.lineItems`, `raw` from `ocr`) in list/get/create. (3) **Image not viewable** — added `GET /receipts/:id/image-url` (user-scoped presigned download via `BlobStore.presignDownload`) + `getReceiptImageUrl` resource; the detail page now renders the image. Verified live: line items returned, image-url fetches 200. api + web typecheck + `next build` green. |
| 2026-06-15 | Content-addressed receipt uploads + OCR retry-status fix (ADR-026) | **Receipts now hash-addressed.** Client computes SHA-256 (`crypto.subtle`) before the direct-to-storage PUT; `presign(contentType, sha256)` derives a content-addressed, per-user key (`receipts/<userId>/<sha256>.<ext>`); `register({imageKey, sha256})` **dedups** (same hash → returns existing receipt, skips re-OCR); the **worker re-hashes** fetched bytes and fails on mismatch (integrity). `sha256` column on `receipts` (migration `0002`, nullable, `(userId,sha256)` lookup index); `ocr` payload stays id-only. Generic `files` table + partial-unique index deliberately deferred (YAGNI). Also folded in the **OCR retry-status fix**: the processor only writes `status:'failed'` on the final BullMQ attempt (`attemptsMade+1 >= opts.attempts`), so a receipt stays `processing` between retries instead of flashing `failed` (the web stops polling on any non-`processing` status). **Verified live** (offline worker): content-addressed key, dedup (same id, no new row), parse, and integrity mismatch → failed; plus the earlier diagnosis that the project `.env` had a malformed/doubled `OPENAI_API_KEY` (corrected locally — git-ignored). `pnpm -r typecheck` ✅, storage 8 + contracts 14 tests ✅, `next build` ✅. ADR-026 added. |
| 2026-06-15 | Split Groups + Remind, seed enrichment, self-person (ADR-024/025) | **(1) Seed** — `@spendlio/db` seed enriched from "user+categories" to a full demo: 4 accounts (balances derived from txns), 23 transactions (June+May 2026, `accountId` + income/recurring/split/pending/cleared statuses), 5 budgets, 4 friends + 1 self-person, 3 groups (+members), 2 even splits (+shares), 1 settlement. Idempotent (fixed UUIDs by namespace + `onConflictDoNothing`). Bug caught: forgot `currency` on txns (NOT-NULL) → added in the insert map. **(2) Groups** — NestJS `groups` module (`GET /groups` → groups with `memberIds`; `POST /groups` validates members are the user's people, 400 otherwise). No contract/DB change (both existed). **(3) Remind** — `POST /people/:id/remind` inserts an in-app `settle_reminder` notification referencing the person (synchronous, no transport yet); 404 for unknown/self ids. **(4) Self-person (ADR-024)** — new `people.is_self` column (migration `0001`), excluded from `GET /people`; the seed uses it as the model-B split payer so friends net out correctly without polluting people lists. `SplitsService.create`/`createExpenseAction` left as-is (modal split-create payer quirk = remaining follow-up). **(5) Web** — `listGroups`/`createGroup`/`remindPerson` resources; `createGroupAction`/`remindPersonAction`; Split page reskinned to the bundle (Groups card w/ AvatarGroup + net badge, People card w/ inline `RemindButton`, `AddGroupForm`). **Verify:** `pnpm -r typecheck` ✅, `@spendlio/ui` 45 tests ✅, `next build` ✅; live curl against seeded API confirmed groups, member-ownership 400, remind ok/404, self hidden, balances Alex/Sam $137.00 + Maya settled. ADR-024 + ADR-025 added. |
| 2026-06-15 | Design follow-ups (post-merge review fixes) | From live review: (1) **Modal portals to `<body>`** — the topbar's `backdrop-filter` was becoming the fixed-position containing block, so dialogs clipped under the header; now centered. (2) **Transactions toolbar** — removed the duplicate in-row "Add expense" button + inline form (topbar modal owns it); just chips + search now. (3) **Rich Add-expense modal** — replaced the embedded plain form with the designed flow (Scan-instead, AmountInput, description, category icon picker, Split toggle + Evenly/Exact/% + per-person checkboxes/amounts); new `createExpenseAction` composes `POST /transactions` + `POST /splits` (no new API; even split of others' portion, you in the divisor; ADR-021 model B). (4) **Separated Assistant + Insights** into two routes/nav items to match the design — `/assistant` = chat, `/insights` = AI insight cards + "Where {month} went" donut (live-derived, no fabricated month-over-month). web typecheck + `next build` green; ui 45 tests green. Known gaps captured above (Split Groups/Remind, seed enrichment). |
| 2026-06-15 | Design-system reconciliation to the canonical Claude Design bundle (ADR-023) | Implemented the real design system over the clean-room build. **Foundation:** replaced `@spendlio/ui/styles.css` tokens with the canonical 8-file system (semantic `--surface-*`/`--text-*`/`--border-*`/`--action-*` layer, full positive/negative/warning/info scales, px type scale, richer radius/shadow/motion); migrated **every** consumer (20 components + all web inline styles) off `--color-*`/`--font-body`/`--motion-*`/`--ease-out` and dropped the old aliases (no back-compat). **Components:** reconciled in place keeping `spl-*`/`data-*`/repo APIs — Button (accent/danger/quiet + lg + brand glow + icon slots), Card (variant/interactive/header), Badge (full tone set + dot), Avatar (xs..xl + AvatarGroup), CategoryIcon (circular chips + canonical hues), new Divider + Modal. **Chrome:** real logomark SVG, bundle-accurate 248px sidebar, sticky backdrop-blurred PageHeader topbar (scroll container in AppShell), Donut + BarChart primitives. **Pages reskinned presentation-only** (data flow untouched): Overview (balance hero/recap/donut/budgets/recent), Transactions, Accounts, Budgets, Split, Assistant, Settings — bundle-referenced pages reskinned via 6 parallel subagents; Receipts/People/Recap inherit the system. **Phase 3:** Modal + topbar Add-expense/Scan modals wired to the real forms (AddTransactionForm `bare`/`onSuccess`, UploadReceipt), presentational `/onboarding` flow. Canonical bundle archived at `docs/design-bundle/`; spec at `docs/superpowers/specs/2026-06-15-design-system-reconciliation-design.md`. **Deviations:** omitted the prototype's dead search box + mock split/OCR/groups data rather than ship non-functional UI; mobile kit out of scope (no mobile app). Whole-repo typecheck + `next build` green; tests **123 passed** (ui 45, +6). `pnpm lint` is a no-op in this repo (no package defines a `lint` script). |
| 2026-06-15 | Plan 06 · Editable Settings + base currency (Slice F) | `UpdateUserInput` narrowed `CreateUserInput.partial()` → `UserSchema.pick({name,defaultCurrency}).partial()` so PATCH can't mutate email/locale/timezone (TDD, contracts 14 green). New `PATCH /me` on `MeController` (scoped to `@CurrentUser().id`, DTO has no `id`, `ZodPipe(UpdateUserInput)`, bumps `updatedAt`). Web: `updateMe()` resource, `updateMeAction` (revalidates `/settings`+`/`), `SettingsForm` (name `Input` + currency `Select` from `Object.keys(CURRENCY_DECIMALS)`; email/locale/timezone read-only), settings page now editable. **Policy deviation from plan:** plan Task 2 wanted to add Vitest to `apps/api` — SKIPPED per the locked surgical test-runner decision; `PATCH /me` verified by typecheck + code-inspected scoping (manual curl smoke pending a live stack). api + web typecheck 0. |
| 2026-06-15 | Plan 05 · People page (Slice E, web-only) | `/people` server page: lists people (Avatar + name + email) joined with `getBalances()` for per-person, **per-currency** balance Badges ("owes you"/"you owe"/"Settled up" neutral); add-person via `createPersonAction` (Zod `CreatePersonInput`, blank optionals→undefined, `revalidatePath('/people','/split')`) + `AddPersonForm` (`useActionState`); People nav entry (`UserPlus`, after Split). No backend/resource/contract changes (all reused). Edit/delete out of scope (no backend route). Per surgical policy, **no apps/web vitest** — action covered by typecheck + manual smoke (skipped the plan's `actions.test.ts`). web typecheck 0. |
| 2026-06-15 | Plan 04 · Settle up (Slice D) | New NestJS `settlements` module (`POST`/`GET /settlements`) mirroring `splits`: user-scoped, verifies both people belong to the user, inserts `status:'settled'`+`settledAt` so the existing `SplitsService.balances()` netting (settled-only) picks it up immediately. **No core/contract change** — `CreateSettlementInput` already existed (Task 1 = confirming test, contracts 10 green); `netForUser` already nets settlement reverse-edges (Task 2 = pinning test, core 36 green). Web: `listSettlements`/`createSettlement` resources, `settleUpAction` server action (major→cents, dual-validate, `revalidatePath('/split','/')`), `SettleUpForm` (`useActionState`), Settle-up card on `/split`. **Token fix:** form used non-existent `--color-negative`/`--color-positive` → corrected to real `--negative-500`/`--positive-500`. api + web typecheck 0. **Manual curl smoke (cross-user isolation + balances-move) still PENDING** — needs a running seeded stack. |
| 2026-06-15 | Plan 03 · Monthly Recap page (Slice C, web-only) | Dedicated `/recap` server page parameterised by `?month=` (defaults to current month, no redirect/`[month]` route); month picker (`Select`) navigates by query; income/expense/net `Stat`s + per-category `CategoryIcon`+`ProgressBar` share-of-total + top merchant; `EmptyState` when recap not built (404) and warn `Notice` when API down (both collapse to `data===null` via `safe`). Insights left intact (assistant lives there). Recap sidebar entry. **Surprise:** plan's verbatim `const [y,m]=month.split('-').map(Number)` fails web's `noUncheckedIndexedAccess` (TS18048) — fixed with destructuring defaults `[y=0,m=1]` in both `recap-utils.ts` and `MonthPicker.tsx`. Per surgical policy, no web vitest (pure helpers typecheck-covered). web typecheck 0. |
| 2026-06-15 | Plan 02 · Receipts page (Slice B, web-only) | `/receipts` list + `/receipts/[id]` detail (server components, `revalidate=0`); status `Badge`; client `PollWhileProcessing` (router.refresh while any receipt is `processing` — no client data fetch); upload dance presign → browser `PUT` (`uploadToPresignedUrl`) → register, split client/server so `x-user-id` never leaves the server; server actions + `revalidatePath`; resources `listReceipts`/`getReceipt`/`presignReceipt`/`registerReceipt` (parsed vs `ReceiptSchema`/local `PresignedUpload`); Receipts nav entry. **"Create transaction from receipt" intentionally omitted** — no backend route exists (documented inline). Per surgical test-runner policy, **no apps/web vitest config added** — the pure PUT helper is covered by typecheck + manual smoke (plan's documented fallback). web typecheck 0. |
| 2026-06-15 | Plan 01 · Accounts page + `GET /accounts/balances` | New `AccountBalanceSchema` (contracts) + pure FX/sum math in `packages/core/accounts.ts` (`sumNet`/`pickRate`/`convertMinor`, minor-units, latest-rate either-orientation, null when no pair) — **10 new core tests + 3 contract tests green**. NestJS `AccountsService.balances(userId)` + `@Get('balances')` route (declared before `:id`); both user-owned reads scoped by `user_id`, `fxRates` global. Web: `getAccountBalances()` resource (parsed vs contract), `/accounts` server page + `'use client'` `AccountsTabs` (currency tabs; "All" shows base-converted rollup, "—" when no rate), Accounts nav link. typecheck 0 across contracts/core/api/web. Account-currency assumption per INDEX (sum raw amounts per account; no per-txn reconversion). |
| 2026-06-15 | Plan 00 · `@spendlio/ui` completion | Restored the (working-tree-deleted) package from HEAD, then added 8 components → **20 total**: IconButton, Tag, Select, Switch, Checkbox, AmountInput, EmptyState, Skeleton (+SkeletonRow). Each: `.tsx` + co-located test + `.spl-*` block in `styles.css` + barrel export; all repo-vocabulary tokens. `pnpm --filter @spendlio/ui test` **39 passed** (11 files), typecheck 0; `pnpm --filter web typecheck` 0 (web resolves all UI imports). Two plan-verbatim type bugs fixed: `Tag.color` and `EmptyState.title` collided with the HTML-attribute base — kept the prop names via `Omit<…, 'color'>` / `Omit<…, 'title'>`. Subagent-driven (1 implementer per component, sequential on shared `styles.css`/`index.ts`). |
| 2026-06-15 | ✅ Build complete + committed to main | Full monorepo green: `pnpm -r typecheck` ✅, `pnpm -r test` ✅ **82 passed** (contracts 3 / core 24 / queue 4 / storage 7 / ui 23 / ai 21), `pnpm lint` ✅. Runtime proven (e2e gate + web render smoke). Landed via logical commits merged `--no-ff` to main. Remaining = deferred (Auth.js / deploy / FX rates) + optional secondary endpoints (settlements / recurring / notifications + their UI writes) + app-level test suites. |
| 2026-06-15 | #12 · API typecheck fix | `@spendlio/ai` barrel/config no longer statically reference `./live`/`@ai-sdk`; live provider lazy-loads via a non-literal dynamic import (`LazyLiveProvider`). `apps/api tsc --noEmit` 303s→OOM-crash ⇒ **1.76s clean**; worker 2.35s; ai self 21 tests; offline `/assistant` still streams exact "$55.50". ADR-022. |
| 2026-06-15 | ✅ #10 · End-to-end proof | Booted api+worker+web on live Postgres/Redis/MinIO. Verified the full path: create txn → **categorize** job (transfer→dining) → `/budgets/status` → **split** (even 3000→1500/1500) → **balances** → **receipt** presign→MinIO PUT→**ocr** job (parsed) → **recap** → **AI assistant streamed "You spent $55.50 on dining in June"** (tool-calling, exact integer cents, `useChat` format). Web renders live data ($42.00/$6.75/dining, $15.00 balance) across all 6 pages; `/api/assistant` proxy 200. Float amount→400; user-isolation enforced; soft-delete hides rows. |
| 2026-06-15 | `apps/web` (Next.js) | App Router, 9 routes, `@spendlio/ui` + 3 fonts; server components/actions (Zod); `/api/assistant` proxy → `useChat`. typecheck + `next build` green; renders live API data. |
| 2026-06-15 | `apps/worker` (BullMQ) | Separate app: ocr→categorize chain, recurring (cron sweep), recap (monthly_summaries), notify. Idempotent (deterministic hyphen jobIds), 3-attempt backoff, failed-set retention (DLQ-equivalent). Live-proven. |
| 2026-06-15 | `apps/api` (NestJS) | All resources + streamed `/assistant` + `/me` / `/people` / `/recaps`. CommonJS via `ts-node`+`tsconfig-paths` (esbuild/tsx can't emit decorator metadata). Zod-validated, `user_id`-scoped, dev AuthGuard. Curl + e2e proven. `tsc --noEmit` is fast + clean (**1.76s**) after #12 fixed the AI-SDK typecheck OOM. ADR-011/019/022. |
| 2026-06-15 | Wave 2 · `packages/storage` + `packages/queue` | architect: `BlobStore` interface + S3 impl (MinIO local ↔ R2/S3 prod by env, presigned PUT, API never proxies bytes); BullMQ connection + typed `enqueue()` over the `QUEUES` registry + `createWorker` factory. typecheck + tests green vs live MinIO/Redis. **ADR-020**. |
| 2026-06-15 | `@spendlio/ai` (AI layer) | Vercel AI SDK, provider-agnostic: deterministic **OfflineProvider** default (no key, no calls) + live Anthropic/OpenAI adapter gated on env; rules-first categorizer, vision OCR via `generateObject`, streaming tool-calling chat (`streamChat`→web `useChat`). 7 tests green. **ADR-011/ADR-019**. Switched from `@anthropic-ai/sdk` per user request (provider-swap + UI streaming). |
| 2026-06-15 | `@spendlio/ui` (design system) | Clean-room from `DESIGN_REFERENCE.md` tokens: `styles.css` full token set + 12 token-driven components (Button, MoneyAmount, TransactionRow, CategoryIcon × 12 keys, ProgressBar, Stat, Toast, …). typecheck + 23 tests green. |
| 2026-06-15 | 05 · `@spendlio/core` | money re-exports + split engine (even/exact/percent, deterministic leftover cent) + balances `netForUser`. 24 tests; property verified: every split sums EXACTLY to total for n = 1..20. |
| 2026-06-15 | 04 · `@spendlio/db` | 16 Drizzle tables (incl. `fx_rates`), migration `0000` applied, idempotent seed = 12 categories + demo user `00000000-0000-0000-0000-000000000001`. typecheck green. **ADR-020**. |
| 2026-06-15 | Team setup | Spawned a 4-specialist agent team (architect/backend/ux/ai). Lead manages installs centrally (fixed a zsh-glob bug that silently dropped runtime deps) + runs an integration gate per wave. API runs CommonJS via `ts-node` + `tsconfig-paths` (esbuild/`tsx` can't emit NestJS decorator metadata). |
| 2026-06-15 | 03 · `@spendlio/contracts` | Zod schemas + `z.infer` types + `Create/Update` DTOs for user, account, category, transaction, budget (+`BudgetStatus`), receipt (+`ReceiptLineItem`), split (Person/Group/SplitShare/Split/Settlement/Balance), recap (MonthlySummary/CategorySpend); `money`/`enums`/`common`/`jobs` (`QUEUES` + payloads). typecheck ✓; 3 vitest tests ✓ (valid parse, float-amount rejected, money helpers). **ADR-018** added: entity shapes were authored from the table list — design-system `contracts/src/` drafts weren't bundled. `recurring_rules`/`notifications` deferred (not in step 03 file list). |
| 2026-06-15 | 02 · Docker & env | `docker-compose.yml` (Postgres 16 + Redis 7 + MinIO + one-shot `minio-setup` for the `receipts` bucket), `.env.example`, git-ignored `.env`. `docker compose up -d` ✓; Postgres healthy + `spendlio` db reachable, Redis `PONG`, MinIO `receipts` bucket created (setup exit 0), console HTTP 200 on :9001. |
| 2026-06-15 | 01 · Monorepo skeleton | `git init` (branch `main`); root `package.json`/`pnpm-workspace.yaml`/`turbo.json`/`.gitignore`/`tsconfig.base.json` + `@spendlio/config` (shared eslint flat config). `pnpm install` ✓, `pnpm typecheck` ✓ (no-op, no packages yet). Deviation: pinned `packageManager` to `pnpm@10.33.4` (installed version) instead of doc's `pnpm@9.12.0` — reproducibility / avoid corepack mismatch. |
